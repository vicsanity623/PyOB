name: Verify Codebase

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.BOT_GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          pip install ruff mypy pytest
          pip install -e .
          pip install types-requests types-chardet

      - name: 🪄 Auto-Fix Linting & Formatting
        run: |
          ruff check --fix src/ tests/
          ruff format src/ tests/

      - name: 🚀 Commit & Push Fixes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🪄 PyOB: Automated Lint & Format Fixes"
          # FORCE the bot's identity to prevent the mismatch
          commit_user_name: "pyob-bot"
          commit_user_email: "pyob-bot@users.noreply.github.com"
          commit_author: "pyob-bot <pyob-bot@users.noreply.github.com>"
          # Force the push to use the bot token
          push_options: '--force'

      - name: Run PyOB Validation Suite
        run: |
          chmod +x check.sh
          ./check.sh
